% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kmaxMC.R
\name{kmaxMC}
\alias{kmaxMC}
\title{Probabilistic k_max at displacement targets (Monte Carlo)}
\usage{
kmaxMC(
  Dn,
  ky,
  p,
  Da = c(2.5, 25, 250),
  p_TARGET = c(0.16, "mean", 0.84),
  ns = 50000
)
}
\arguments{
\item{Dn}{numeric, displacements paired with ky and p (same length)}

\item{ky}{numeric, yield acceleration (same length as Dn)}

\item{p}{character/numeric labels; must include numeric quantiles and "mean"}

\item{Da}{numeric vector of displacement targets (e.g. c(2.5,25,250))}

\item{p_TARGET}{numeric/character vector of probability levels to report
(e.g. c(0.16,"mean",0.84))}

\item{ns}{integer, Monte Carlo size (default 5e4)}
}
\value{
data.table with columns: Da, p, kmax
}
\description{
Builds a sample of Dn(ky) from the provided partial quantiles and inverts
Dn(ky)=Da to obtain the distribution of k_max for each target Da.
For each simulation, one Uniform(0,1) is drawn and reused across all ky.
For every ky, ln-quantiles are reconstructed with buildQSpline() from the
numeric p-rows, then rescaled to match the provided "mean" at that ky.
The simulated Dn(ky) is forced non-increasing in ky before inversion.
}
